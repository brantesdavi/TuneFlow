import { HorizontalListComponent } from './horizontal-list.component';

describe('HorizontalListComponent', () => {
  let component: HorizontalListComponent;

  beforeEach(() => {
    component = new HorizontalListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
