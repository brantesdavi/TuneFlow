import { HorizontalListModule } from './horizontal-list.module';

describe('HorizontalListModule', () => {
  const module: HorizontalListModule = new HorizontalListModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
